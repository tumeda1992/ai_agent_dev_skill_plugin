# {拡張子なしファイル名}
{テーブル名} テーブルと、対応するActiveRecordである {アクティブレコードのクラス名} についてこのファイルで述べる

## 役割
{役割や概略レベルの利用時の補足や注意事項などを記載}

## テーブル情報
### カラム
{以下のテーブルに記載。
カラム名は 各サービスの structure.sql(例:rapi/db/structure.sql)やマイグレーションファイル、見れるのであればdbを見にいけばわかる。
railsデフォルトの、id, created_at, updated_atは記載不要。
内容については、specファイルや処理で入れている定数などで推測できれば「（AI生成のため要確認）」の文言とともにたたき台を記載}

| カラム    | 内容  |
|--------|-----|
| `xxx`  | xxx |

### 関連テーブル
- {関連テーブル。外部キーで関連していたら、ここに載せる可能性が高いが、外部キーでつながっていなくてもつながりが深いテーブルであれば載せる}
    - {補足があれば載せる}

### 公開メソッド
#### {メソッド名。インスタンスメソッドであれば`#xxx`、クラスメソッドであれば`.xxx`。scopeであれば`scope: xxx`}
{メソッドの概略説明}
シグネチャ: ({n個目の引数名}: {n個目の引数の型}): {戻り値。意味がわかることが優先で、型が分かりわかりやすいが、説明文でも可能}
