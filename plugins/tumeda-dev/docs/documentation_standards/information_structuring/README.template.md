# このディレクトリの役割

<!--
ドメイン直下に置く README の雛形（詳細は information_structuring/README.md §2）。< > を置き換え、この HTML コメントは消す。
README はドメインの索引であり初見の入口。構造を提案・合意する工程では、この形でモジュール構成を下書きして提案・合意する。
読者名（概観読者・改修読者…）は本文に露出しない——「読む順の目安」は"関わり方"で書く（標準 core_readers）。
-->

<ドメイン名> のナレッジを扱う。<利用者にとって何であり、何を集約するドメインか。1〜3 文>。

<!-- 別ドメインへ委譲するもの（隣接ドメイン・商品種別別・API 層）があれば「外に出すもの」として明記し、ドメイン境界を可視化する（取り込まず接続の事実まで）。構造合意時の足切りに対応。無ければこの段落ごと削除。 -->
**外に出すもの**（接続の事実まで／詳細は各所へ）: <隣接ドメイン・商品種別別・API 層など。例: 配送 → 配送ドメイン、商品種別別 → 別ディレクトリ>。

## とりあえずこれを見ておけばいいもの

- `overview.md`: <ドメインの全体像。まずここ>

## 読む順の目安

読み手の関わり方で入口が変わる（下ほど深い。上の内容は下でも前提になる）。

- **<初めて知る / 境界の他システム関係者>**: `overview.md` だけで完結
- **<使うが変えない>**: `overview.md` → <...>
- **<現行コードを変える>**: <...>
- **<仕様を変える>**: 上記 + <histories 等>

## ナレッジモジュール一覧

<!--
ナレッジモジュール（＝概念の深掘り。overview が持ち出した概念のうち単独で切り出す深さのあるもの）を 1 つずつ見出し化して説明する。overview で骨子として導線される概念も、overview では軽くしか触れない概念も、深ければここに挙げる。
※ ここに挙げるのは「概念の深掘り」だけ。テーブル（*.model.md）・値辞書・testing・histories は概念モジュールでない別カテゴリなので、ここには挙げず「直下のファイルやディレクトリ」に載せる。cases も overview・モジュールと並ぶ独立カテゴリで、ここには挙げない。
各モジュールに「何を扱うか」「なぜ独立が要るか（overview のどの概念の深掘りか／どの関わり方の読み手が引くか）」を書く。ここが構造合意の対象。
※ どのモジュールを立てるか・ディレクトリか単一ファイルか・どう並べるかは、このドメインのモジュール群の性質から決める。他ドメインの箱をそのまま真似ない。深さが無ければモジュールにせず overview か隣接モジュールの一節へ畳む。
-->

### <モジュール名>

<何を扱うか／overview のどの概念の深掘りか／どの関わり方の読み手が引くか／なぜ独立が要るか。モジュールの数だけ繰り返す>

## 直下のファイルやディレクトリ

<!-- 直下の全アイテム（overview.md・cases・上記モジュール・単純なテーブルファイル・補助ディレクトリ）を 1 行ずつの索引に。ナビ用（上のモジュール一覧が「なぜ・何を」、こちらが「どこに何があるか」）。 -->

- `overview.md`: <一言>
- `cases/`: <一言>
- `<module>/`: <一言>
- <…直下に並ぶもの全部…>

## 正本ルール

<!-- 判断・決定の最終記録はどこか。このディレクトリが管理するもの（変わりにくい業務仕様）と、コードでのファクトチェック方針。 -->

<判断の最終記録の所在／このディレクトリが管理する範囲／ファクトチェックの方針>
