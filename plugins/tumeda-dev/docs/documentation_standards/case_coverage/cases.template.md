# ケース（あり得るケースの網羅・抜け漏れ検証用）

<!--
この雛形の使い方（詳細は case_coverage/README.md）:
- 章は「起きること別」（予約する・変更する・キャンセルする 等）。掛け合わせ・状態遷移の"軸"は考える道具で、章名・見出しにしない。
- 章の冒頭で「分岐のエッセンス」（何がどの軸でどう枝分かれするか）を説明しきる。個々のケースは、その分岐の確認と、ケースごとの期待値を見る場。
- 各ケースは見出し＋本文。観点は先に宣言し、各ケースはその穴埋め。
- 記法は内容の形で選ぶ（流れ→図・掛け合わせ→表・並列→箇条書き・理由→散文）。図を出すなら説明（散文・箇条書き）を添える。1 記法に固定しない。
- 深い詳細（仕組み・データ・式）は各モジュールへリンクで譲る（再説明しない）。ケース自体の理解に要る情報は書く。
- overview ＋ このファイル群だけで全体像が掴めることを目標に。< > を置き換え、この HTML コメントは消す。
-->

<ドメイン名> で「起こりうること」を漏れなく並べ、網羅を検証する。深い詳細は各モジュールへ譲る。データ整合性（認可・冪等性・独立性）は状態遷移ではないので別ファイル（`data_integrity.md`）に置く。

## 概要 — 全体像と、起きること別の地図

<!-- ドメインの背骨（基本の流れ／状態遷移）を、内容に合う記法で示す。流れ・状態遷移なら図（＋説明を添える）、規則的な対応なら表。図だけで済ませない。 -->

<背骨の説明。必要なら図（＋説明）や表を添える>

起こりうることを「起きること別」に並べる。各項目は**何で枝分かれするか**の要点だけ示す（中身は各章／各ファイル）:

- **<起きることA>** — <何で枝分かれするか> → <章 or ファイル>
- **<起きることB>** — <何で枝分かれするか> → <章 or ファイル>

各ケースは〈**<観点1>・<観点2>・<観点3>**〉で書く。網羅は「<軸> × <軸>」の掛け合わせと状態遷移で裏で確かめる（軸は見出しに出さない）。

---

## <起きることA>

<!-- 章の冒頭で、この章がどの軸でどう枝分かれするかの"エッセンス"を説明しきる。
     記法は分岐の形で選ぶ（記法標準）: 第一候補は補足を子に持つ階層箇条書き（親＝枝、子＝補足説明）、
     規則的な対応なら表、枝がシンプルなら素の箇条書きか図（図なら説明を添える）。
     ケースを読む前に、読み手が分岐の全体像を掴める状態にする。 -->

<この章の分岐のエッセンス（第一候補: 補足を子に持つ階層箇条書き。表が向けば表、シンプルなら素の箇条書き or 図＋説明）>

### <ケースID>. <ケース名>

<起きていること・影響・対応（宣言した観点）を要点で。>

### <ケースID>. <ケース名>

<… 分岐の数だけケースを並べる …>

## <起きることB>

（同様）

---

## 対象外（あり得ない／扱わない組合せ）

<!-- 欠落が意図的だと示す欄。「なぜ起こらないか」を書く。 -->

- **<組合せ>**: <なぜ対象外か>

## 抜け漏れ検証の使い方

1. 各章の冒頭で示した分岐に、対応するケースが揃っているか確認する。
2. 無ければ「対象外」に該当するか確認。該当しなければ抜け漏れ。
3. 掛け合わせ・状態遷移で組んだ網羅を、QA レビュアーの目（隠れ状態・並行/順序・値境界・書き忘れた不変条件）で崩しにいく。
4. ケースを具体化する過程で新たな分岐が出たら、章冒頭の説明とケースに足す。
